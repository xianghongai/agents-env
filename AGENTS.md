# AGENTS.md

本文件记录 `agents-env` 仓库自身的维护约束。

## 边界

- `setup-instructions.sh` 只负责 Instructions 入口文件，不含任何 Skills 逻辑；Skills 的获取与软链全部由 `setup-skills.sh` 承担。
- **注册表内容当前是测试数据**，用于验证协议与脚本；各 Agent 的机制会随版本变化，真实数据由后续维护者负责。本阶段交付的是数据协议与脚本机制。
- **脚本不内置任何「谁支持什么」的判断。** 声明要软链就照投，说不要就不管，不追问原因。协议形状也不从当前样本数据反推：所有条目在某字段上取值恰好一致，不构成收窄该字段的理由。
- 厂商差异一律进 `config/agents.json`，不进脚本流程。新增或调整 Agent 不应改动 `setup-instructions.sh`。
- **具体的厂商名、Agent 标识符与其私有路径，只允许出现在 `config/agents.json` 里。** 脚本、测试、文档一律不得写死某一家：厂商会改名、会停运、会被收购，清单随时增删，而这三处不该因此改动。需要举例时用占位形式（`<agent-id>`、`.<agent>/rules/<入口文件>`、`<AGENT>.md`）。`.agents/` 这类跨厂商公共约定不在此列，可以直接点名。
- 对某一家的查证结论（读了哪份文档、得出什么路径）记进该条目的 `references`，不要写进通用文档。结论会过期，放在条目旁边才知道跟着谁一起删。
- 路径一律相对于 Scope 根，不写绝对路径，保证可在任意用户环境与任意仓库中使用。

## 脚本约束

- `setup-instructions.sh` 必须自包含、可 `curl`/`wget` 管道执行；**不引入共享库文件**，公共逻辑宁可重复。
- 运行期所需的仓库资源按需下载到临时目录，退出时清理；`--source` 指向本地检出时不联网。
- 同一份脚本须在 Bash 与 Zsh 下行为一致，并保持 Bash 3.2 语法兼容：不使用关联数组、`mapfile`、大小写参数展开、`BASH_SOURCE` 等较新能力。
- 不使用在 Zsh 中具有特殊含义的变量名，`path`、`status`、`argv`、`cdpath`、`fpath` 等一律避开。
- 不依赖通配符在两种 Shell 下的一致行为；枚举目录内容用 `find` 写入文件后读取。
- 零参数进入交互向导，出现任意参数即严格非交互；交互输入固定读 `/dev/tty`；`--help` 无任何副作用。

## 副作用

- 入口文件直接幂等：内容一致跳过，不一致备份为 `.local.bak` 后覆盖。
- **备份一律用 `.local.bak` 后缀**，不用 `.bak`：项目级安装写入的是 Git 仓库，而常见 `.gitignore` 含 `*.local.*` 规则，能避免备份件被误提交。
- Skills 软链使用相对路径，保证目标整体移动后仍然有效。
- 遇到同名实体目录一律跳过并报告，绝不覆盖（各 Agent 自带的系统 Skill 即此形态）。
- `--prune` 只删符号链接，不递归删除目录。
- 任何校验失败都必须在产生副作用之前中止，不留部分结果。

## 测试

- **测试只测核心逻辑，不测清单内容。** 承上述边界：断言里不得出现任何 Agent 标识符或其路径，期望值一律从注册表现场推导（按 scope、按 mode、按路径深度挑选），增删 Agent 不应该需要改动 `test/`。
- 推导落空会让断言空转而假通过，因此每个测试文件开头都要把前提钉死，缺少所需形态时直接非零退出，不静默跳过。
- 所有 Bash/Zsh 测试只在一次性 Docker 容器中进行，宿主不执行被测脚本。
- 测试环境自包含在 `test/compose.yaml` 中，不依赖本机既有的其它容器项目。
- 容器默认无网络、非特权、源码只读挂载；涉及下载的路径不走真实网络。
- 报告须区分容器内 Linux 运行结论与 macOS/BSD 静态审计，不得把前者表述为后者已验证。

## 安全与交付

- 不写入真实域名、账号、Token、证书或个人敏感信息。
- 未经明确要求，不提交、不推送、不发布。
