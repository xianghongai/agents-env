# {{TITLE}}

本文件只负责指路，不承载项目规则，也不复制任何内容。

1. **项目上下文**：先完整阅读仓库根目录 `AGENTS.md`，它是本项目事实、架构、命令、所有权与验证约束的唯一来源。
2. **按需能力**：任务相关的工作流与领域规范位于 `.agents/skills/<name>/SKILL.md`。先按 frontmatter `description` 匹配当前任务，再读取正文与 `references/`。
3. **冲突处理**：任何指引与 `AGENTS.md`、项目配置或源码不一致时，以后者为准。
